export const selectType = (state) => state.filterType;

export const selectAlle = (state) => state.alle;

export const selectBeskjed = (state) => state.beskjed;

export const selectOppgave = (state) => state.oppgave;

export const selectInnboks = (state) => state.innboks;

export const selectSearch = (state) => state.filterSearch;

export const updateSearch = (state) => state.search;

export const selectBeskjedList = (state) => state.beskjedList;
export const selectSetBeskjedList = (state) => state.setBeskjedList;
export const selectRemoveBeskjed = (state) => state.removeBeskjed;

export const selectInaktivBeskjedList = (state) => state.inaktivBeskjedList;
export const selectSetInaktivBeskjedList = (state) => state.setInaktivBeskjedList;
export const selectAddInaktivBeskjed = (state) => state.addInaktivBeskjedList;
